/** Minimum flashcards per deck for effective revision. */
export const MIN_FLASHCARD_COUNT = 20;

export const FLASHCARD_MORE_PROMPT =
  'Generate at least 20 more unique flashcards (front/back) on this topic. Return JSON with a flashcard card containing 20+ new pairs.';
